# name: Backend CI/CD

# on:
#   workflow_dispatch:
  
#   push:
#     branches:
#       - main
#     paths-ignore:
#      - '.github/workflows/**'

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#     steps:
#       - uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

      
#       - name: Cache Docker layers
#         uses: actions/cache@v3
#         with:
#           path: /tmp/.buildx-cache
#           key: ${{ runner.os }}-buildx-${{ github.sha }}
#           restore-keys: |
#             ${{ runner.os }}-buildx-

#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ghcr.io
#           username: ${{ github.repository_owner }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Docker meta
#         id: docker_meta
#         uses: docker/metadata-action@v4
#         with:
#           # TODO: Replace YOUR_ORG and YOUR_REPO with your GitHub org and repo name
#           images: ghcr.io/YOUR_ORG/YOUR_REPO
#           tags: |
#             type=raw,value=${{ github.ref_name }}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v3
#         with:
#           context: .
#           file: ./Dockerfile
#           platforms: linux/amd64
#           push: true
#           tags: ${{ steps.docker_meta.outputs.tags }}
#           labels: ${{ steps.docker_meta.outputs.labels }}
#           cache-from: type=local,src=/tmp/.buildx-cache
#           cache-to: type=local,dest=/tmp/.buildx-cache,mode=max

#   deploy:
#     # TODO: Replace 'your-runner-label' with your self-hosted runner's label,
#     # or use 'ubuntu-latest' if deploying from GitHub-hosted runners.
#     runs-on: your-runner-label
#     needs: build
#     steps:         
#       - name: Pull latest image
#         run: |
#           set -e
#           # TODO: Replace YOUR_ORG/YOUR_REPO with your actual image name
#           docker pull ghcr.io/YOUR_ORG/YOUR_REPO:main
  
#       - name: Docker Compose Restart
#         run: |
#           set -e
#           docker compose down 
#           docker system prune -f
#           docker compose up -d 


