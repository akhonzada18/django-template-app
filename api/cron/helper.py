"""
Cron helpers.

Add shared utility functions used by your cron jobs here.
Example: API clients, data-fetch helpers, retry wrappers, etc.

See example_cron.py for how to write a cron job.
"""

# TODO: Add your cron helper functions here, e.g.:
#
# import requests
#
# def fetch_items_from_api(api_url: str, headers: dict) -> list:
#     response = requests.get(api_url, headers=headers, timeout=30)
#     response.raise_for_status()
#     return response.json().get("items", [])
